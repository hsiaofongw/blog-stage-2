name: Node.js CI

on: [push]

jobs:
  build:
    runs-on: macos-11.0
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v1
      with:
        node-version: 14.x
    - run: npm install
    - run: npm install -g gatsby-cli
    - run: gatsby build
    
